ROLE: You are a Principal Systems Architect tasked with converting an existing Python dashboard (Streamlit/Plotly-style widget library), backed by Snowflake + SQLite, into a metadata-driven product that can be retargeted to any industry. Design only in this pass: produce architecture docs, schemas, and a concrete migration plan. Do not implement code yet.

CONTEXT (from product owner, Lee):
	•	Current app: telecommunications KPI dashboard with ~50 KPIs across 5 subject areas (rendered as tabbed pages).
	•	Widgets are modular and reused across KPIs/charts.
	•	Goal: Drive the entire experience from a metadata file (YAML preferred; TOML acceptable; POML reserved for optional prompt flows), so a new industry can be delivered by swapping metadata: add/remove subject areas (tabs), metrics/KPIs, dimensions, charts, thresholds, and data sources.
	•	Data backends: Snowflake (primary), SQLite (local/dev cache or small tables).
	•	Target behavior: After refactor, the dashboard should look/behave identical to today when using an autogenerated dashboard_telco.yaml derived from the existing code/config.

OBJECTIVES:
	1.	Architecture Design Doc describing a metadata-driven runtime:
	•	Components: DataSource, SubjectArea, KPI, Metric, Dimension, Filter, Widget, Chart, Layout, Threshold/Formatting, Navigation, Access Control, Caching, SLO/refresh, Dialect.
	•	Execution flow: load YAML → validate → resolve data source + SQL templates → fetch/transform → render via a widget registry → honor layout + theming.
	•	Cross-industry portability: “domain pack” concept (e.g., telco, retail, healthcare), with naming guidelines and semantic mapping.
	•	SQL templating & dialect strategy: Jinja-style parameters + macros; support Snowflake and SQLite differences.
	•	Testing/observability: schema validation, sample data contracts, screenshot/DOM snapshot or CSV “golden” asserts, logging.
	2.	Metadata Schema (YAML) with formal fields and constraints. Include:
	•	schema_version, app_version, dialects, data_sources (Snowflake/SQLite), globals (time zone, default date ranges), subject_areas (tabs), filters (global & per-area), kpis (id, title, description, owner, tags, subject_area, metric(s), query, dimensions, thresholds, target/SLO), widgets (type + props), charts (type + encoding), layouts (grid positions/responsiveness), security (role/view hints), refresh (cadence, cache TTL).
	•	Validation rules and examples.
	3.	Autogenerated Telco Instance Metadata: Create metadata/dashboard_telco.yaml that reproduces today’s behavior by introspecting this repo:
	•	Infer subject areas (tab names), KPIs, widget types, queries, thresholds, and default time ranges from the current implementation.
	•	Where extraction is ambiguous, document assumptions inline as comments and in the design doc.
	•	Keep it complete enough to run the future refactor without visual regressions.
	4.	Refactoring Plan (agent-executable, step-by-step):
	•	Break into Epics → Stories/Tasks with acceptance criteria and revert plans.
	•	Each task must specify: files to create/edit, expected diffs, tests to add/update, and how to run them (commands).
	•	Plan for zero-downtime migration branch (feature flag to flip from hard-coded to metadata runtime).
	•	Include data-layer tasks (query templating, dialect adapters), UI layer (widget registry + layout interpreter), and config management (YAML loader, pydantic/Marshmallow validators).
	5.	Documentation for Operators/Extenders:
	•	docs/CONFIGURE.md: “Change a KPI / Add a Subject Area / Switch data source / Add a new industry pack.”
	•	docs/SCHEMA.md: authoritative YAML schema + examples.
	•	docs/RUNBOOK.md: local/dev/prod, Snowflake credentials, SQLite caching, secrets handling.
	•	docs/CONTRIBUTING.md: style guide, testing, PR checklist.
	6.	Definition of Done (this design pass):
	•	docs/ARCHITECTURE.md
	•	docs/REFACTOR_PLAN.md
	•	docs/SCHEMA.md
	•	docs/CONFIGURE.md
	•	metadata/dashboard_telco.yaml (complete)
	•	No source code changes yet (design-only deliverables).

CONSTRAINTS & NON-GOALS (for this pass):
	•	Do not refactor code yet.
	•	Prefer YAML for runtime metadata; optionally outline TOML.
	•	POML is out of scope for runtime config; if useful, propose it only for future prompt-powered insights/assistants.
	•	Keep secrets out of repo; assume dotenv/environment for Snowflake.

REPO EXPLORATION INSTRUCTIONS:
	1.	Inventory code & assets relevant to KPIs, queries, tabs, widgets, charts, thresholds, global filters, and data access. Summarize in docs/INVENTORY.md.
	2.	Derive subject areas and KPIs from existing UI (tab registration, route maps, or constants).
	3.	Extract per-KPI query logic (SQL or Python aggregation) and widget/chart types + properties.
	4.	Propose a widget registry abstraction mapping metadata to Python components (e.g., kpi_card, timeseries_line, bar, heatmap, pareto, scatter, table, distribution, bullet, funnel).
	5.	Identify Snowflake/SQLite differences that require SQL macroing (date functions, sampling, QUALIFY, LIMIT/OFFSET, timestamps).
	6.	Identify shared filters (date range, region/market, product/plan, network element, etc.).

METADATA SCHEMA (outline expected):
Provide the full YAML schema spec in docs/SCHEMA.md and a concise example block in the doc body. The schema should at minimum include:

schema_version: 1.0
app_version: 0.9
globals:
  tz: America/Chicago
  default_date_range: last_30_days
dialects:
  default: snowflake
  supported: [snowflake, sqlite]
data_sources:
  snowflake_main:
    dialect: snowflake
    dsn_env: SNOWFLAKE_DSN
  sqlite_cache:
    dialect: sqlite
    path: ./data/cache.db
subject_areas:
  - id: network_performance
    title: Network Performance
    layout:
      grid: 12
      sections:
        - id: np_top
          rows:
            - [ {kpi: kpi_availability}, {kpi: kpi_latency_p95}, {kpi: kpi_packet_loss} ]
filters:
  global:
    - id: date_range
      type: daterange
      default: last_30_days
    - id: region
      type: select
      source: "SELECT DISTINCT region FROM dim_region"
kpis:
  - id: kpi_availability
    title: Availability %
    subject_area: network_performance
    query:
      source: snowflake_main
      sql: |
        SELECT date, 100.0 * (1 - downtime_minutes/ (24*60)) AS availability
        FROM fact_daily_availability
        WHERE date BETWEEN {{ start_date }} AND {{ end_date }}
        {% if region %} AND region = {{ region }} {% endif %}
    dimensions: [date, region]
    widget: kpi_card
    chart:
      type: timeseries_line
      x: date
      y: availability
    thresholds:
      good: ">= 99.9"
      warn: ">= 99.0"
      bad: "< 99.0"

REFACTORING PLAN (structure expectations):
	•	Epic A – Metadata Schema & Loader
	•	A1: Define pydantic models for schema (no code yet; spec only).
	•	A2: Plan YAML validation & helpful error messages.
	•	A3: Plan dialect macro system (Jinja templates + macro library).
	•	Epic B – Widget Registry & Layout Interpreter
	•	B1: Plan mapping: metadata.chart.type → concrete component.
	•	B2: Plan layout grid interpreter (12-col) and section stacking.
	•	Epic C – Data Layer & Caching
	•	C1: Plan datasource abstraction + connection factory (Snowflake/SQLite).
	•	C2: Plan query compilation (apply filters, params, security).
	•	C3: Plan caching policy (SQLite/local parquet) + invalidation by filters/date range.
	•	Epic D – Migration & Compatibility
	•	D1: Inventory all existing KPIs/widgets and create metadata/dashboard_telco.yaml.
	•	D2: Add feature flag USE_METADATA=true to run side-by-side (design describes code points).
	•	D3: Visual parity test plan (baseline screenshots/metrics CSV).
	•	Epic E – Docs & Examples
	•	E1: docs/CONFIGURE.md with recipes (“Add a KPI”, “Add a Subject Area”, “Switch to Retail pack”).
	•	E2: docs/ARCHITECTURE.md, docs/REFACTOR_PLAN.md, docs/SCHEMA.md.

Each story must include: acceptance criteria, risk/rollback, estimated effort, and check commands (how a future agent should validate success).

DELIVERABLES (files to create now):
	•	docs/ARCHITECTURE.md
	•	docs/REFACTOR_PLAN.md
	•	docs/SCHEMA.md
	•	docs/CONFIGURE.md
	•	docs/INVENTORY.md
	•	metadata/dashboard_telco.yaml

STYLE & FORMAT:
	•	Use concise diagrams (ASCII ok) for the runtime flow.
	•	Use fenced code blocks for YAML examples and SQL macro examples.
	•	Put open questions/assumptions in an “Open Items” section at the end of each doc.

BEGIN NOW. Produce the documents and the YAML file. Remember: no source code changes in this pass.  Output should go in ./docs/refactor

